# Livestock Portal — reconciled union of Beef Weight, Beef Cutout,
# Livestock Inventory, CME Feeder Cattle Index, and Seasonal Futures &
# Spreads' individual requirements.
# plotly pinned <6 to match livestock_inventory's constraint; kaleido pinned
# to 0.2.1 since newer plotly/kaleido combos silently break Cloud PNG export.
streamlit>=1.38.0  # st.container(key=) — the st-key-* CSS the tiles and watermarks rely on — landed in 1.38
pandas>=2.0.0
numpy>=1.24.0
plotly>=5.18.0,<6.0.0
kaleido==0.2.1
requests>=2.31.0
urllib3>=2.0.0
openpyxl>=3.1.0
snowflake-connector-python>=3.12  # cme_feeder_cattle DB backend when USE_SNOWFLAKE=1
cryptography>=42  # imported directly by snowflake_db to convert a PEM key to DER

psycopg2-binary>=2.9.0  # beef_weight/livestock_inventory read the shared NASS cache (usda-nass-etl)

pypdf>=5.0  # letter/sources.py parses AMS report 3208, which is published only as a PDF
msal>=1.30  # letter/mailbox.py reads the subscription digests from Ross's own inbox via Graph
